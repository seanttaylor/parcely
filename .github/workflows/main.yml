name: Run Unit & Integration Tests

on:  
  pull_request:
    branches:
      - master
    tags:
      - "v*.*.*.*"

jobs:
  build-and-push:
    name: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: "[STEP] Checkout"
        uses: actions/checkout@v2

      - name: "[STEP] Setup pipeline environment variables"
        run: |
          echo "::set-output name=MY_GIT_SHA::$(git rev-parse --short "$GITHUB_SHA")"
          echo "::set-output name=MY_IMAGE_TAG::${GITHUB_REF#refs/*/}"

      - name: "[STEP] Set Node.js version"
        uses: actions/setup-node@v1
        with:
          node-version: 12.x

      - name: "[STEP] Install app dependencies"
        run: npm install

      - name: "[STEP] Run Integration Tests"
        run: npm test
        env:
          JWT_SECRET: superSecret
          PLATFORM_OUTBOUND_EMAIL_USERNAME: jayde.anderson@ethereal.email
          PLATFORM_OUTBOUND_EMAIL_PASSWORD: N4kJ5KKtTcTSH4AsUT